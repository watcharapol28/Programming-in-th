#include<bits/stdc++.h>
using namespace std;


int a[700010];
int b[700010];


int main()
{
    ios_base::sync_with_stdio(0);cin.tie(0);
    int m, k;
    for(int i = 0; i < m; i++)
    {
        cin >> a[i];
    }
    for(int i = 0; i < k; i++)
    {
        cin >> b[i];
    }
    
}